// Not required for test files

import 'package:auravibes_ui/ui.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Ui', () {
    test('can be instantiated', () {
      expect(const Ui(), isNotNull);
    });
  });
}
